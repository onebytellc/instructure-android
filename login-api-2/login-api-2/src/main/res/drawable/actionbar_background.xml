<?xml version="1.0" encoding="utf-8"?>
<!-- set the normal state to be transparent because we change the
    background color when masquerading -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:state_pressed="true"
        android:drawable="@color/canvasBlueSelectedTransparent" />
    <item

        android:drawable="@android:color/transparent" />
</selector>